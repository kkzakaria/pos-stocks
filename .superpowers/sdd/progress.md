# Ledger Phase 1 — feat/phase-1-fondations
Task 1: complete (commits 26ee596..9f393f5, review clean — @types/node ajouté au brief)
Task 2: complete (commits 9f393f5..3f5917d, review clean — vitest-pool-workers ^0.12, fixes .prettierrc/shadcn justifiés)
Task 3: complete (commits 3f5917d..6aac6aa, review clean après fix commentaire auth-cli). Minor à trier en revue finale: index unique dupliqué organization.slug (artefact CLI, drizzle/0000_sad_shotgun.sql:109-110)
Task 4: complete (commits 6aac6aa..3962f05, review clean — adaptation hooks.before better-auth 1.6.23 vérifiée)
  Minor à trier en revue finale: test 403 n'asserte pas le message FR exact (auth.test.ts) ; createAuth ré-instancié à chaque requête (accepté, note perf)
Task 5: complete (commits 3962f05..eb7c8c2, review clean après fix erreurs stables + onError global)
  Minors à trier en revue finale: message anglais better-auth passé tel quel dans CREATION_UTILISATEUR (setup.ts:113) ; classification d'erreur par includes("UNIQUE constraint failed") non commentée ; user orphelin résiduel possible sur course double-setup (accepté) ; comparaison SETUP_TOKEN non constant-time (accepté)
Task 6: complete (commits eb7c8c2..ecdfd98, review clean)
  Minor à trier en revue finale: branche membership: null non testée (me.ts:20)
Task 7: complete (commits ecdfd98..e7c5760, review clean, E2E navigateur validé par l'implémenteur)
  Minors à trier en revue finale: setState après unmount possible dans LoginForm au succès (nit hérité du brief) ; erreur sans role=alert/aria-describedby ; api.ts créé mais pas encore consommé (attendu)
Task 8: complete (vérifications locales OK, hooks actifs, PR #1 créée, CI verte en 32s ; fix churn routeTree.gen.ts exclu de prettier/eslint, commits 2293923+30c2725)
Task 9: complete (D1 prod a95a8921, secrets posés, API + SPA déployés et vérifiés: health ok, setup 201→409, sign-in+me ok en prod ; CD GitHub: ACCOUNT_ID + VITE_API_URL posés, CLOUDFLARE_API_TOKEN à créer manuellement par l'utilisateur)

## Fix revue finale

Trois correctifs issus de la revue finale, appliqués en un seul commit sur `feat/phase-1-fondations`.

**Changements**
1. `apps/api/src/routes/setup.ts` — la garde de jeton de setup échouait ouverte quand `SETUP_TOKEN` n'était pas défini (les deux côtés `undefined`, comparaison `!==` passait). Ajout de `!c.env.SETUP_TOKEN ||` en tête de condition ; la réponse 403 `INTERDIT` est inchangée. Ajout du commentaire demandé par le reviewer au-dessus du test `includes("UNIQUE constraint failed")` expliquant que D1 n'expose pas de code d'erreur structuré.
2. `.github/workflows/deploy.yml` — ajout d'une étape « Vérifier le secret CLOUDFLARE_API_TOKEN » juste après « Vérifier que les apps existent » et avant `setup-bun`, qui échoue explicitement (`::error::` + `exit 1`) si le secret est absent, avec le message d'aide pour le créer.
3. `docs/superpowers/plans/2026-07-08-pos-stocks-roadmap.md` — ajout en tête de la liste « Phase 2 — Administration » d'un item de reprise couvrant : étape de migration D1 dans `deploy.yml`, message français sur `CREATION_UTILISATEUR`, suppression de `--passWithNoTests` et des dépendances web inutilisées, redirection `/login` si déjà connecté, `role="alert"` sur l'erreur de connexion, suppression de l'index dupliqué `organization_slug_uidx`, documentation de `COOKIE_DOMAIN` dans `.dev.vars.example`.

**Preuves d'exécution**
- `bun run --cwd apps/api test` → 4 fichiers de test, **10/10 tests passés** (health, auth, me, setup — setup.test.ts couvre la garde modifiée).
- `bun run typecheck` → `tsc --noEmit` sur web, api et packages/shared, **exit 0**.
- Web tests non exécutés (hors périmètre, fichiers non touchés) ; ils ont néanmoins tourné automatiquement via le hook pre-push (`bun run --cwd apps/web test`) et sont passés (2/2).

**Commit** : `07a2b70` — `fix: garde fail-open SETUP_TOKEN, échec explicite CD sans token, reprise Phase 2 dans la roadmap` — poussé sur `feat/phase-1-fondations` (6e5e2d0..07a2b70).

Note : le hook pre-commit (prettier/eslint) a reformaté la condition de garde sur plusieurs lignes ; le contenu logique est inchangé.
Revue finale whole-branch (81dcbda..07a2b70): Ready to merge = YES après fix 07a2b70 (garde fail-closed SETUP_TOKEN, préflight CD, reprise Phase 2 dans roadmap). Tous les minors différés tracés dans la roadmap Phase 2.
Revues CodeRabbit (2026-07-09): bot PR (4 findings) + CLI (8 findings, 2 doublons) → fix ce28316 (timing-safe token, cleanup user orphelin, LoginForm try/catch, apiFetch timeout+corps vide, garde CORS WEB_ORIGIN, trim setupSchema, gitignore web/.wrangler). Écartés documentés: updatedAt default (schéma généré), réordonnancement org-avant-user (briquerait les retries), codes erreurs structurés D1 (inexistants). Vérification: check CodeRabbit PR #1 vert sur ce28316 ; 2e passe CLI abandonnée (bloquée en preparing_sandbox >30min). Prod redéployée (API 5e355d91, web 7a2669f9). Tests 16/16 api + 3/3 web.

# Ledger Phase 2 — feat/phase-2-administration
P2 Task 1: complete (commits 26fb5e5..ff37568 + fix roadmap, review clean)
P2 Task 2: complete (commits 3150f96..2c08e9f, review clean, spot-check migration OK)
  Notes: updatedAt toujours sans défaut SQL (dette ouverte) ; colonnes user nullable côté SQL (risque faible documenté, defaultValue appliqué côté Better Auth + DEFAULT SQLite)
P2 Task 3: complete (commits 2c08e9f..b6cc04d, review clean, 0 issue)
P2 Task 4: complete (commits b6cc04d..bd7ca60, review clean après fix sécurité anti cross-tenant mandaté par le scan auto — garde org avant bypass + test dédié + eslint ignore .wrangler)
  Minors: membership lu 4x dans requireWarehouseRole (style) ; lookup warehouses sur chaque appel (coût accepté)
P2 Task 5: complete (commits bd7ca60..3fcfb55, review clean)
P2 Task 6: complete (commits 3fcfb55..17a53eb, review clean, 0 issue)
P2 Task 7: complete (commits 17a53eb..3fe9419, review clean — 5 risques sécurité vérifiés OK)
  Minors: test manquant « admin ne désactive pas owner/admin » sur /statut ; biais modulo négligeable dans generateProvisionalPassword (hérité du plan)
P2 Task 8: complete (commits 3fe9419..114e897, review clean — inclut refactor estViolationUnicite qui répare la détection UNIQUE silencieusement cassée de setup.ts)
  Minor: pas de garde anti-cycle sur la marche .cause de estViolationUnicite (plafond de profondeur à ajouter — revue finale)
P2 Task 9: complete (commits 114e897..a0283d5, review clean, 0 issue)
P2 Task 10: complete (commits a0283d5..21aa3c4, review clean — 4 risques sécurité vérifiés OK)
  Minor: nit de formatage d'imports dans mon-compte.ts (cosmétique, prettier normalisera)
P2 Task 11: complete (commits 21aa3c4..be21360, review clean — shadcn base-mira via CLI + utils; minor: doubles espaces cosmétiques dans ui/badge,button,utils)
P2 Task 12: complete (commits be21360..42641dc, review clean, 0 issue)
P2 Task 13: complete (commits 42641dc..8f8d686, review clean — adaptation DialogTrigger render= base-ui; minor colSpan 5 vs 4 pour auditor)
P2 Task 14: complete (commits 8f8d686..2226a2c, review clean, 0 issue)
P2 Task 15: complete (commits 2226a2c..71c55c6, review clean, 0 issue)
P2 Task 16 (en cours): suite complète verte (36 api + 8 web + typecheck + lint) ; E2E navigateur 8/8 validé (owner: boutique + employé + mdp provisoire + affectation ; employé: redirection forcée mon-compte, changement mdp, pas d'Administration ; owner: paramètres enregistrés, désactivation → connexion refusée)
  Notes UX pour revue finale: (a) après changement de mdp, la session courante est révoquée (notre route ne relaie pas le nouveau cookie better-auth) → retour login après succès ; (b) le login affiche « Identifiants invalides » même quand l'API dit 403 « Compte désactivé »
P2 Task 16: complete — revue finale fable « Ready to merge: YES » après vague de fix (5 commits 8ed9fa6..6ec5660) :
  CRITICAL résolu: plugin organization bloqué (option + hard block hooks.before + test) ; IMPORTANTS résolus: index unique member_user_uidx (migration custom 0002), normalisation casse emails (Zod transform + hook + tests), rollback signUpEmail dans users.ts ; triage pré-merge soldé (test admin/statut owner, login 403 « Compte désactivé », passe prettier).
  Minors différés (Phase 3): autoSignIn false, mapping APIError mon-compte (max 128), race metadata organization, beforeLoad role guard routes admin front, helper validerCorps, INTERDIT vs ACCES_REFUSE, split utilisateurs.tsx, plafond profondeur estViolationUnicite, colSpan auditor, session révoquée après changement mdp (relayer set-cookie), biais modulo mdp provisoire.
  Bilan suite: 39 api + 8 web + typecheck + lint verts ; E2E navigateur 8/8.

# Ledger Phase 3 — feat/phase-3-catalogue
P3 Task 1: complete (commits 1c230cb..0e28eec, review clean — découverte : autoSignIn:false ⇒ réponse synthétique anti-énumération better-auth sur email dupliqué, pre-check ajouté dans setup.ts)
  Minors P3: setupSchema.email non lowercased (pre-check setup.ts sensible à la casse — même classe que users.ts pré-P2fix) ; statut 409 codé en dur dans le retry setup (vs err.statusCode)
P3 Task 2: complete (commits 0e28eec..773a72e, review clean — bucket R2 créé et vérifié sur le compte)
P3 Task 3: complete (commits 773a72e..69f8ae9, review clean, 0 issue)
P3 Task 4: complete (commits 69f8ae9..a39e440, review clean, 0 issue — 46 api)
P3 Task 5: complete (commits a39e440..525a05c, review clean — 51 api)
  Minors: pas de test auditor en écriture (allowlist sûre) ; pas de test cross-org suppliers (chemin identique categories)
P3 Task 6: complete (commits 525a05c..2ade2ad, review clean — 57 api)
  Minors: filtre actifs=false ignoré (lecture raisonnable du brief) ; collision via SKU dérivé -STD → 409 SKU_EXISTANT imprécis mais conforme
P3 Task 7: complete (commits 2ade2ad..feb0ea2, review clean — 62 api)
  Minor: lookups produit par productId seul dans variants.ts (défense en profondeur org à ajouter si l'invariant faiblit)
P3 Task 8: complete (commits feb0ea2..dfc3d40, review clean — 66 api)
  Minors: pas de sniffing magic-bytes (allowlist 3 MIME bénins, non requis) ; pas de rejet explicite de « .. » dans :fichier (clé validée en DB, non exploitable)
P3 Task 9: complete (commits dfc3d40..10e7632, review clean après fix invalidation ["products"] + apiFetch fieldErrors — amélioration globale des messages de validation)
  Minor: alt="" sur les miniatures produits
P3 Task 10: complete (commits 10e7632..717bda8, review clean)
  Minor: badge expiré compare UTC vs instant local (offset ~0 en Afrique de l'Ouest, hérité du plan)
P3 Task 11: complete (commits 717bda8..b4d9a8c, review clean, 0 issue)
P3 Task 12 (en cours): suite complète verte (66 api + 12 web + typecheck + lint) ; E2E navigateur validé : owner (catégorie Boissons, fournisseur, produit Coca 500/400 trackLots → PRD-0001 auto, image uploadée+affichée, variante Pack de 6 → PRD-0001-PACK-DE-6 + Standard désactivée, lot LOT-2026-07) ; staff (mdp provisoire → changement forcé → catalogue lecture seule liste+fiche, 0 bouton d'écriture, champs disabled)
P3 Task 12: complete — revue finale fable « Ready to merge: YES » après vague de fix (ac43069..4fc7ba3 + spec SKU non modifiable) :
  Importants résolus: scoping variantes GET /products, garde content-length pré-parseBody, CYCLE_CATEGORIE (marche ancêtres bornée 20), bascule implicite par attributes (plus de reconstruction -STD), setupSchema email lowercase, tests matrice (auditor 403, cross-org products/suppliers 404). 71 api + 12 web + typecheck + lint verts (re-vérifiés indépendamment par le relecteur).
  Minors différés « Phase 4 prep »: helpers org-scopés partagés, hook usePeutEcrire, décomposition $productId.tsx (685 lignes), unicité barcode à décider AVANT le POS, escape LIKE %/_, cache-control images (bust liste), put-before-delete image, race DERNIERE_VARIANTE hors batch, supplierUpdateSchema non nullable (impossible de vider contact/phone), baisse de price vs minPriceOverride variantes, actifs=false ignoré, 409 dur retry setup, alt miniatures, badge expiré UTC.

# Ledger Phase 4 — feat/phase-4-moteur-de-stock (plan: docs/superpowers/plans/2026-07-10-phase-4-moteur-de-stock.md, base 2c08642)
P4 Task 1: complete (commits 2c08642..f7fc272, review clean — Approved, spec ✅)
  Minors: commentaire garde prix légèrement trop large (toute modif, pas que baisse) ; fournisseurExiste sans consommateur jusqu'aux Tasks 7-9 (voulu par le plan)
P4 Task 2: complete (commits f7fc272..4ab15ce, review clean — Approved, spec ✅, extraction iso-comportement vérifiée)
  Minors: pattern remontage key={produit.id} de SectionInfos à surveiller si réutilisé sur des sous-arbres lourds (Tasks 11-13) ; reformatage prettier cosmétique
P4 Task 3: complete (commits 4ab15ce..5cbda21, review clean — Approved, spec ✅ ; dédup barcode idempotente, CHECK détectable via estViolationCheck, snapshots sans fuite d'index custom — vérifiés par le relecteur)
P4 Task 4: complete (commits 5cbda21..820f169 + doc 99a4a6d, review Approved, spec ✅)
  Important résolu par documentation: fenêtre TOCTOU inter-tables produit↔variante assumée par commentaire « LIMITE ASSUMÉE (v1) » dans barcode.ts (99a4a6d) — pas de contrainte SQLite possible, fenêtre négligeable en saisie back-office, POS Phase 6 devra tolérer un doublon résiduel
  Minors: littéral BARCODE_EXISTANT dupliqué 8× (style, mandaté par le brief) ; pas de test de course réelle sur le filet catch (convention du dépôt) ; pas de test PATCH barcode:null
P4 Task 5: complete (commits 99a4a6d..8d1b5ad + fix 8e32fb7, review Approved après re-revue)
  DÉVIATION ACCEPTÉE (documentée dans stock.ts): l'upsert unique INSERT ON CONFLICT DO UPDATE du plan est cassé sous SQLite (CHECK évalué sur la ligne brute AVANT résolution du conflit) → remplacé par INSERT DO NOTHING (ligne neutre qty 0) + UPDATE relatif dans le même batch ; toutes propriétés préservées (atomicité, CHECK sur quantité résultante, CMP pré-update, pas de lost-update)
  Importants résolus (8e32fb7): estViolationCheck(err, fragment?) discriminé sur stock_levels_quantity_positive (un CHECK d'une autre table dans instructionsAvant rethrow inchangé — test d'intégration dédié) ; test ligne fantôme (premier mouvement négatif sur niveau inexistant → rollback complet, pas de ligne résiduelle)
  Minors: calculerDeficits filtre par variantId seul en SQL (rattrapé en JS) ; pas de test net négatif agrégé même variante ; table aux_check_test sans DROP (isolation vitest)
P4 Task 6: complete (commits 8e32fb7..fa18e27 + fix bde2c41, review Approved)
  Déviation acceptée: tiebreak desc(rowid) sur le tri du journal (created_at à la seconde) — vérifiée sûre (table rowid, append-only) et scopée
  Important résolu (bde2c41): tests paramétrés admin/auditor/stock_manager sur la branche { tous: true } de porteeLectureStock (vérifié par le contrôleur, diff test-only)
  Minors (triage revue finale): /movements et /alerts ne 404 pas sur warehouseId cross-org (hérité du plan, /levels le fait — incohérence) ; regex du/au sans validité calendaire (2026-13-40 → Invalid Date lié) ; porteeLectureStock calculée avant les validations 400 dans /movements
P4 Task 7: complete (commits bde2c41..72bebbc, review clean — Approved, spec ✅, 0 Important ; point d'écriture unique et ordre garde/bypass vérifiés dans le code)
  Minors: message top-level "Données invalides" vs « Le motif est requis » du plan (convention validerCorps partagée, pré-existante — wording nested dans details.fieldErrors) ; delta 0 non testé e2e ; 404 plat variantId inexistant non testé (chemin partagé avec cross-org)
P4 Task 8: complete (commits 72bebbc..c3b3ac5 + fix cc75010, review Approved)
  Déviation acceptée: items.length === 0 vs !item (convention no-unnecessary-condition du dépôt) ; règles de lot re-évaluées sur valeurs effectives fusionnées au PATCH (plus rigoureux que le brief)
  Important résolu (cc75010): DELETE ligne — pré-lecture avant batch, un 404 ne bump plus updatedAt (vérifié par le contrôleur sur le diff, test avec lecture DB directe)
  Minors (triage revue finale): cascade suppression brouillon avec lignes non testée (FK onDelete cascade) ; itemId cross-document non testé (filtre and() couvrant) ; quantity<=0/unitCost négatif non assertés e2e (Zod partagé)
P4 Task 9: complete (commits cc75010..ad764b2 (2 commits), review clean — Approved, spec ✅, 0 Important ; UPDATE statut sans filtre + trigger vérifiés, CMP recalculé à la main par le relecteur, réutilisation de lots dédupliquée dans le document)
  Déviations acceptées: | null sur lireNiveau (piège eslint, convention du fichier) ; assertion journal supplémentaire au test d'immuabilité
  Minors (triage revue finale): caissier non testé explicitement sur la validation (auditor l'est, même chemin de refus) ; expiryDate divergent entre deux lignes même (variantId, lotNumber) → la première gagne silencieusement (non traité par le brief non plus — commentaire ou règle métier à poser)
P4 Task 10: complete (commits ad764b2..5699bf2, review clean — Approved, spec ✅, 0 Important ; deux sens d'écart couverts, avgCost préservé par omission du set, dry-run prouvé sans effet)
  Déviation acceptée: retrait du guard if (premiere) redondant (précédent applyMovements même fichier)
  Minors (triage revue finale): matrice reconcile pas entièrement pinnée par ses tests (admin 200 / auditor 403 manquants) ; branche applicable:false (somme négative) sans test dédié ; multi-écarts simultanés non testés ; sens « ligne orpheline sans mouvements → 0 » non testé
P4 Task 11: complete (commits 5699bf2..30df827, review clean — Approved, spec ✅, 0 Important ; miroir de permissions front/API vérifié rôle par rôle, exact)
  Minors (triage revue finale): pas de garde client delta≠0 (400 API surfacé quand même) ; pas d'état isError/retry sur les listings (gap pré-existant sur tous les écrans catalogue — cleanup transverse futur)
P4 Task 12: complete (commits 30df827..f918b9e, review clean — Approved, spec ✅, 0 Important ; contrat API vérifié champ par champ, queryKey stock-movements raccordée à l'invalidation de la Task 11)
  Minors (triage revue finale): pas d'état isError (même gap que /stock — cleanup transverse) ; libellé « Page 5/3 » transitoire possible si le total baisse ; delta 0 s'afficherait en rouge sans signe (impossible métier)
P4 Task 13: complete (commits f918b9e..9a58030, review clean — Approved, spec ✅, 0 Important ; schémas Zod alignés champ par champ, trackLots à source unique, received = lecture seule totale vérifiée sur tous les chemins)
  Minors (triage revue finale): pas d'état d'erreur de query sur les écrans détail (gap transverse pré-existant) ; variantId non resynchronisé si la recherche exclut la variante choisie (cosmétique, serveur revalide) ; pas de tests automatisés des écrans (convention du projet)
P4 Task 14 (en cours): suites 118 api + 14 web + typecheck + lint verts ; migrations 0004/0005 locales OK ; réconciliation dry-run vérifiée ({"ecarts":[],"applique":false}) ; E2E navigateur 7/8 ✅
  ÉCART E2E (étape 4, fix en cours): formulaire produit sans champ defaultMinStock → badges Stock bas jamais déclenchés via seuil produit (API/schémas OK, gap web uniquement)
  Anomalies E2E (triage revue finale): pas d'UI de retrait d'affectation entrepôt (dette Phase 2, repli scénario utilisé) ; bouton Se déconnecter nécessite scroll (accessibilité) ; validation de réception via confirm() natif vs modale in-app
  Fix E2E (59513fe): champ Seuil d'alerte par défaut ajouté aux formulaires produit (création + édition + type Produit) — re-vérification E2E étape 4 intégralement ✅ (héritage « 10 (produit) », surcharge entrepôt 2, badges ligne + sidebar) → E2E global 8/8
  Note automation: PATCH seuil avec fill "" = no-op (Ctrl+A+Delete fonctionne) — à vérifier si champ vide est bien transmis null (probable artefact outil)
P4 Task 14: complete — revue finale fable « Ready to merge: YES » (0 Critical, 0 Important) après vérifications transverses (point d'écriture unique confirmé par grep exhaustif, matrice uniforme sans route orpheline, migration 0005 idempotente sûre pour la prod, snapshots sans fuite)
  Retouches pré-merge appliquées: sidebar sticky h-screen (213e990, bouton déconnexion toujours visible) ; champ seuil vidé → null vérifié dans le code (stock/index.tsx:118 — l'anomalie E2E était un artefact de l'outil)
  Triage revue finale: tout le reste différé Phase 5 (ticket « harmonisation contrat lecture stock » : 404 cross-org movements/alerts, validité calendaire dates, isError transverse, beforeLoad /stock ; + décision métier expiryDate divergent, ancrage estErreurDeclencheur, UI retrait d'affectation) ou ignoré (style/brief-mandaté)
  Rappel Phase 6: fenêtre TOCTOU barcode inter-tables assumée — le POS doit tolérer un doublon résiduel
  Bilan: 118 api + 14 web + typecheck + lint verts ; E2E navigateur 8/8 ; réconciliation dry-run vérifiée
  Vague de fix CodeRabbit (57a863c api + 791081b web) — 7 findings, tous fondés :
    Majors: garde LOT_EXISTANT réparée (fragment lots.variant_id — le nom d'index ne figure pas dans le message SQLite, test capturant le format réel) ; reconcilier auto-cohérent (somme du journal recalculée dans le statement d'écriture, plus de lost update) ; champs vidés → null dans l'édition produit (description/categoryId/barcode/minPrice)
    Minors: validité calendaire du/au (400 sur 2024-02-30 — solde un différé Phase 5) ; reset input file après upload ; formatDateJour sans décalage UTC pour les péremptions ; reset delta/motif à l'ouverture du dialogue Ajuster
    Bilan après vague: 119 api + 16 web + typecheck + lint + build verts
  Note CI: timeout vitest relevé à 20s (bb52c58) après 2 échecs de runners partagés (5s trop juste pour les matrices scrypt+D1) ; 1 échec « Network connection lost » workerd transitoire (re-run vert)
  CodeRabbit incrémental: 1 minor (sûreté du retry) RÉFUTÉ empiriquement (test de contrôle : isolatedStorage réinitialisé entre tentatives — bootstrapOwner passe en retry x1) — réponse postée sur la PR
PHASE 4 MERGÉE (PR #5, 2026-07-11) — deploy success, prod vérifiée : health ok, SPA 200, 4 tables stock + 6 triggers présents sur la D1 remote

# Ledger Phase 5 — feat/phase-5-transferts-inventaires (plan: docs/superpowers/plans/2026-07-11-phase-5-transferts-inventaires.md, base 78a29cf)
P5 Task 1: complete (commits 78a29cf..a62e829, review clean — Approved, spec ✅, 0 Important ; ancrage estErreurDeclencheur vérifié compatible avec les 4 appelants RECEPTION_VALIDEE, branches sans warehouseId byte-identiques)
  Minors: injectivité de la clé composite non re-commentée au nouveau call site ; pas de test pathologique estErreurDeclencheur("SQLITE_CONSTRAINT")
P5 Task 2: complete (commits a62e829..f089688, review clean — Approved, spec ✅, 0 Important ; garde /stock miroir exact de useAccesStock, pas de boucle de redirection, DELETE affectation org-scopé vérifié côté API)
  Déviation acceptée: narrowing data.xxx imposé par no-unnecessary-condition (6 occurrences, vérifiées sûres une à une — pas de placeholderData)
  Minors: pas de test du beforeLoad ni du flux de retrait (branches nouvelles non couvertes) ; disabled global des badges × pendant une mutation (cosmétique, mandaté par le brief)
P5 Task 3: complete (commits f089688..a0a907e, review clean — Approved, spec ✅, 0 Important ; snapshots sans fuite vérifiés, triggers testés négatif ET positif, migration rejouable IF NOT EXISTS)
  Concern adjugé: exports nommés ajoutés à shared/index.ts — convention établie (précédent c3b3ac5 Phase 4), la note du brief était fausse, le contrat Interfaces fait foi
  Minors (note pour Tasks 6-7): WHEN de transfers_expedie_fige NULL-unsafe (status=NULL passerait — aucun chemin ne le fait) ; liste de colonnes du trigger transfer_items sans id/organization_id (défense en profondeur théorique)
P5 Task 4: complete (commits a0a907e..3bf31e0, review clean — Approved, spec ✅, 0 Important ; élargissement chirurgical TYPES_APPORT_VALORISE, invariants intacts, 4 CMP recalculés à la main par le relecteur)
  Minors: pas de test purchase+transfer_in dans le MÊME batch (chemin identique au test 2×purchase existant, vérifié analytiquement) ; pas d'assertion transfer_in delta négatif / unitCost 0 (code partagé avec purchase)
P5 Task 5: complete (commits 3bf31e0..352f1d4, review clean — Approved, spec ✅, 0 Important ; portée bi-entrepôt sans fuite cross-org vérifiée, ordre des gardes conforme au modèle purchases, frontières send/receive/DELETE respectées)
  Minors: verifierReglesLot sans details (optionnel) ; ordre 403/400 sur GET / avec statut invalide + warehouseId hors portée (gap de couverture) ; portée SQL de GET / et transfertLisible TS synchronisés à la main (risque de divergence future — helper partagé à envisager)
P5 Task 6: complete (commits 352f1d4..2ba0129, review clean — Approved, spec ✅, 0 Important ; ordre du batch prouvé (gel CMP avant statut avant mouvements), course double-expédition testée sur état réel, extraction stock-erreurs.ts iso-comportement)
  Minors: littéral STATUT_INVALIDE dupliqué pré-check/catch (constante partagée à envisager) ; lot d'une autre variante non re-vérifié au send (gardé à l'ajout de ligne) ; quantités 0/négatives non exercées ici (validation Zod amont)
P5 Task 7: complete (commits 2ba0129..0c1815c, review clean — Approved, spec ✅, 0 Important ; CMP recalculés à la main (100 et 150 exacts), exemption received_quantity du trigger vérifiée, double réception séquentielle testée, unitCost ?? 0 prouvé défensif)
  Minors: combinaison destination-valorisée + écart partiel vérifiée à la main mais non pinnée par test (le biais CMP documenté y est le plus visible) ; course concurrente réelle non exercée (harnais mono-thread) ; multi-lignes à quantités mixtes non testé
P5 Task 8: complete (commits 0c1815c..c7898ea, review clean — Approved, spec ✅, 0 Important ; filtre sent-only vérifié, scope SQL pas post-filtrage JS, joins 1:1 sans fan-out)
  Minors (gap du plan, triage revue finale): pas de test multi-lignes/multi-transferts même destination (vérifié par inspection) ; caissier non testé sur /transit (porteeLectureStock hérité)
P5 Task 9: complete (commits c7898ea..7ba5842 + fix b08c4d1, review Approved après re-revue)
  Importants résolus (b08c4d1): figeage d'ouverture en VRAI INSERT…SELECT dans le batch (le code du brief contredisait la contrainte globale du plan — la contrainte a fait foi) ; test exerçant l'index partiel par insert direct DB + estViolationUnicite("inventory_counts.warehouse_id")
  DÉCOUVERTE (documentée): db.run(sql brut) casse le batch D1 en drizzle 0.44 (SQLiteRaw sans .stmt) → insert().select() avec champs sql aliasés ; IDs de lignes générés en SQL par lower(hex(randomblob(16))) (128 bits, aucune contrainte de format sur les id — hétérogénéité cosmétique vs UUID JS)
  Minors: branche catch du handler non traversable séquentiellement (pré-check intercepte — maximum atteignable testé) ; fenêtre théorique document zéro ligne si stock_levels se vide entre pré-check et batch (dégénéré) ; ligne d'un AUTRE inventaire non testée (scope countId vérifié par inspection)
P5 Task 10: complete (commits b08c4d1..fe2e6f1, review clean — Approved, spec ✅, 0 Important ; formule d'écart net recalculée à la main (−1 vs −4 naïf), delta relatif au commit vérifié dans le SQL du service, double clôture par trigger testée, écart nul exclu — évite un 500 du service)
  Minors (triage revue finale): enrichissement sku/nom post-commit — un échec transitoire rendrait un 500 alors que la clôture est déjà commitée (le GET détail garde l'état) ; limite v1 quantiteAvantCloture documentée seulement en commentaire (possibleRace dans le recap à envisager plus tard)
P5 Task 11: complete (commits fe2e6f1..c8257c0, review clean — Approved, spec ✅, 0 Important ; correspondance bouton-par-bouton vérifiée sur les 7 verifierAccesEntrepot de l'API, preparerReception iso-contrat Zod, déviations type-only validées avec précédents)
  Minors (triage revue finale): destinations du dialogue limitées aux entrepôts visibles (l'API accepte toute l'org — limitation v1 documentée, itération future) ; alert() natif sur supprimerLigne.onError (pattern préexistant receptions) ; couverture preparerReception extensible (3+ lignes)
P5 Task 12: complete (commits c8257c0..de967ee, review clean — Approved, spec ✅, 0 Important ; shapes vérifiés contre les handlers réels, saisies isolées par ligne (write sites tous tracés), déviations eslint iso-comportement prouvées)
  Minors: pluriel français « 0 compté » (motif préexistant) ; risque de fuite saisies entre inventaires moot (remontage par navigation — commentaire si lien direct futur)
P5 Task 13 (en cours): suites 175 api + 23 web + typecheck + lint verts ; migrations 0006/0007 locales OK ; E2E navigateur 14/14 ✅ après fix bd1a0f3
  FIX E2E (bd1a0f3): GET /warehouses/destinations (tout membre org, entrepôts actifs id+name+type) + sélecteur destination du formulaire transfert — l'arbitrage « destinations limitées aux entrepôts visibles » du plan rendait la création inutilisable pour le manager local (contredit par le propre scénario E2E du plan)
  Anomalie UX non bloquante (récurrente): window.confirm() natifs (déjà triée « ignorer v1 » en Phase 4)
P5 Task 13: complete — revue finale fable « Ready to merge: YES » après vague de fix cdbb0c5 (0 Critical restant)
  IMPORTANT résolu (cdbb0c5): TOCTOU brouillon→expédition fermé — gel de quantity dans le batch de send (out=in garanti), migration 0008 (trigger transfers_send_lignes_gelees LIGNE_NON_GELEE sur pending→sent avec ligne unit_cost NULL ; + transfer_items_expedie_update élargi à id/organization_id via DROP/CREATE rejouable), réception défensive 500 ERREUR_INTERNE sur unit_cost NULL, refine dédup itemId sur transferReceiveSchema — re-verdict: fermeture validée point par point, aucune régression
  Résidus notés par la revue (DIFFÉRÉS Phase 6, gravité moindre): PATCH lotId concurrent non gelé (divergence d'annotation de lot au journal, sans effet quantités/valorisation) ; DELETE de ligne concurrent = sortie tracée sans entrée (perte visible au journal, pas de stock fantôme) ; TOCTOU jumeau sur la validation des réceptions Phase 4 (pas de stock ex nihilo — même passe corrective)
  Triage revue finale: différés Phase 6 = helper de portée partagé (avant POS), test du biais CMP écart partiel (avant ventes), enrichissement post-commit clôture (try/catch 5 lignes), tests caissier /transit, test purchase+transfer_in même batch, multi-lignes quantités mixtes, trigger BEFORE DELETE documents (défense en profondeur), index unique (transfer_id,variant_id,lot_id), refine details verifierReglesLot — tout le reste ignoré (justifications dans la revue)
  Bilan: 178 api + 23 web + typecheck + lint verts ; migrations 0006-0008 locales OK ; E2E navigateur 14/14 ✅
PHASE 5 PRÊTE À MERGER — PR à ouvrir
  Vague de fix CodeRabbit PR #6 (ea334f2 + 2bdb45a + 2dcbd6c): migration 0009 (BEFORE DELETE documents terminaux TRANSFERT_TERMINE/INVENTAIRE_CLOS, transitions pending verrouillées STATUT_INVALIDE, created_at gelé sur lignes expédiées — 5 tests de verrous ; le trigger a cassé le raccourci pending→received d'un helper de test, rerouté par sent) ; reset destination au changement d'origine (web) ; notes d'archivage plans P4/P5. Bilan: 183 api + 23 web verts, db:generate idempotent, 0009 locale OK
PHASE 5 MERGÉE (PR #6, 2026-07-12) — deploy success, prod vérifiée : health ok, SPA 200, 4 tables transferts/inventaires + 22 triggers sur la D1 remote (migrations 0006-0009)
AJOUT DIFFÉRÉS PHASE 6 (2026-07-12): issue GitHub #7 — unicité des noms (warehouses/products/categories/suppliers) : index uniques org-scopés COLLATE NOCASE via migration custom + dédup préalable des données existantes, 409 NOM_EXISTANT via estViolationUnicite (colonnes), affichage formulaires web, tests (doublon/cross-org/casse) — à embarquer en DÉBUT de Phase 6 avec le ticket des différés (décision utilisateur)
PHASE 6 — POS (plan docs/superpowers/plans/2026-07-12-phase-6-pos.md, branche feat/phase-6-pos, base 676f7a5)
Task 1: complete (commits 3fd70a3..e07daac, review clean après 1 fix)
  Fix Important: dédup migration 0010 cassée à 3+ doublons NOCASE même org (sous-requête corrélée relisant la table mid-UPDATE) → réécrite en CTE AS MATERIALIZED + ROW_NUMBER sur état initial (e07daac), double preuve empirique (bun:sqlite + wrangler d1 --local), re-revue Approved
  Minors (différés revue finale): chemin de dédup 0010 non couvert par un test CI (preuve manuelle seulement) ; try/catch NOM_EXISTANT dupliqué 8× (style établi du dépôt) ; meta/ drizzle committés (accepté — précédent 0005-0009, comportement inhérent à generate --custom)
Task 2: complete (commits e07daac..f229a36, review clean, 0 issue — 3 risques nommés vérifiés par lecture: ordre batch gels→statut→mouvements, trigger sur ligne insérée, non-interférence triggers 0005)
  Minor (différé revue finale): gelsLignes = N UPDATE par réception à N lignes (motif hérité 0008, limite de taille de batch D1 si volumes)
Task 3: complete (commits f229a36..b13d150, review clean, 0 issue — équivalence prouvée: De Morgan sur estDansPortee, or() mono-arg vérifié dans drizzle installé, porteeLectureStock intouché)
Task 4: complete (commits b13d150..a9da4f6, review clean, 0 issue — CMP 129 et 120 re-dérivés à la main par le relecteur, try/catch borné au post-commit vérifié)
  Minor (différé revue finale): test multi-lignes n'épingle pas avgCost destination (opportunité, pas un écart au brief)
Task 5: complete (commits a9da4f6..133c666, review clean, 0 issue — trigger LOT_INSUFFISANT vérifié sans off-by-one (BEFORE INSERT, NEW inclus dans la somme), index partiels exacts, immuabilité UPDATE/DELETE only, sale_items porte unitPrice+catalogPrice+sourceWarehouseId+lotId? pour T6/T10/T12)
  Minor (différé revue finale): les scénarios forts du rapport (rollback multi-statements, no-fire positif/sans-lot) reposent sur un probe supprimé — non couverts par les tests committés
Task 6: complete (commits 133c666..5f5ab4f, review clean, 0 issue — refine intacts: somme paiements ===, unicité variantId|sourceWarehouseId, référence mobile money obligatoire, receivedAmount interdit hors cash)
Task 7: complete (commits 5f5ab4f..e1a625b, review clean, 0 issue — sous-requête cash scoped session+méthode vérifiée sur schéma, signe de l'écart confirmé, matrice write/read conforme spec §4, /current résolu serveur)
  Minors (différés revue finale): double lookup warehouse POST / (verifierAccesVente + boutiqueScope) ; lookup warehouseMembers GET / sans filtre organizationId (inoffensif, incohérence de motif) ; gap de couverture hérité du brief: auditor org GET / après refus POST, manager/auditor local voient tout vs cashier les siennes
Task 8: complete (commits e1a625b..1a87b8c, review clean, 0 issue — filtres org/actif en SQL, dépannage borné org+actif+qty>0+boutique exclue, pas de N+1, résolution barcode variante>produit implicite vérifiée)
  Minors (différés revue finale): pas de test cross-org direct sur les 2 routes POS lecture (couvert transitivement) ; filtre variante inactive non testé isolément
Task 9: complete (commits 1a87b8c..5c99b63, review clean, 0 issue — tri FEFO + tie-break lotId testés, lots vidés exclus, org-scoping fermé par construction FK (warehouseId mono-org))
  Minors (différés revue finale): ORDER BY expiryDate SQLite met NULL en premier (inoffensif, allouerFefo re-trie, mais commentaire du rapport inexact) ; titre de test « nul ou négatif » sans fixture négative ; pas de test multi-entrepôts de lireLotsDisponibles
Task 10: complete (commits 5c99b63..e4b4242, review fable Approved, 0 Critical/Important — 5 named risks vérifiés contre le code réel: ordre batch (stock.ts:266), idempotence doublement scoped org, retry sur état relu borné à 1, zéro écriture hors batch, extraction eslint iso-comportement)
  Minors (différés revue finale): TROU DE NORMALISATION dédoublonnage (variante sans sourceWarehouseId + même variante avec sourceWarehouseId===storeId passe Zod → même couple résolu → LOT_INSUFFISANT déterministe → CONFLIT_CONCURRENT insoluble côté client ; fail-safe mais cul-de-sac UX — normaliser sourceWarehouseId ?? storeId avant le refine T6 ou dans la route) ; sale non null-checké avant réponses (inatteignable en pratique, code du brief) ; chemins retry/CONFLIT_CONCURRENT et SESSION_FERMEE non testés (pas de harnais de concurrence)
Task 11: complete (commits e4b4242..4f1e28f, review clean, 0 issue — bornes du jour = motif stock.ts existant, matrice lecture ⊇ écriture vérifiée sur les deux axes, extraction venteBoutique iso-comportement)
  Minors (différés revue finale): limite fixe 200 tickets sans pagination (non testée au-delà) ; borne haute « demain » couverte par lecture de code, pas par test
BLOC B (backend POS) TERMINÉ — 243 tests api verts, 11 tâches/16
Task 12: complete (commits 4f1e28f..3061fb7, review clean, 0 issue — miroir plancher structurellement vérifié contre sales.ts T10, arithmétique entière sans division, trou de normalisation T10 hérité non aggravé (aucun chemin T12 ne le déclenche))
  ⚠️ résolu par le contrôleur: l'hypothèse « T8 aplatit priceOverride/minPriceOverride dans ArticlePos » est confirmée — le contrat T8 revu verbatim contre le brief inclut les champs plats price/minPrice
  Minors (différés revue finale): changerPrix no-op silencieux sur entrée malformée ; definirSource sans test direct (gap du brief) ; type local MeLike au lieu d'importer Me (drift possible) ; commentaire de garde à ajouter près de definirSource (ne jamais passer storeId comme source — trou T10)
Task 13: complete (commits 3061fb7..6506e62, review clean, 0 issue — chemin caissier+mdp provisoire tracé bout en bout sans boucle, /pos hors _app confirmé au routeTree, sidebar gate exclut auditor/stock_manager)
  Note: walkthrough navigateur du brief non exécuté par l'implémenteur — compensé par lecture des gardes ; à couvrir dans l'E2E de phase avant revue finale
  Minors (différés revue finale): apiFetch destinations sans try/catch dans beforeLoad ; session.isError non modélisé (dégrade en OuvertureCaisse, brief-mandaté)
Task 14: complete (commits 6506e62..6a6885d, review clean après 1 fix)
  Fix Important x2 (défauts du code du brief, corrigés = conformité plan restaurée): resteAPayer délégué à lib/pos.ts (iso-comportement vérifié contre T12) ; sémantiques de dialogue manuelles complètes sur ModalePaiement (role/aria-modal/labelledby/focus initial/piège Tab/Escape) — écart au Dialog partagé justifié et validé (DialogContent max-w-sm inadapté au plein écran, autorisation du brief)
  Minors (différés revue finale): piège de focus artisanal — 2 fuites aux bords (Shift+Tab depuis le conteneur, échappée pointeur sans inert) ; regex non ancrée résiduelle panier.test.tsx:442 ; Number(montantMobile) sans garde NaN
Task 15: complete (commits 6a6885d..7025fed, review clean, 0 issue — idempotence vérifiée sur pièces client+serveur (régénération uniquement onSuccess, y compris dejaEnregistree), mapping 409→alertes conforme stock-erreurs.ts, dépannage exclu boutique côté serveur, re-pointage de ligne après choix de source)
Task 16: complete (commit 3150158, code du brief repris verbatim — ticket-recu.tsx, tickets-du-jour.tsx, fermeture-caisse.tsx + branchement ecran-vente.tsx) — Phase 6 clôturée
  Décisions d'architecture reportées (header du plan, valables pour toute la phase) : (1) FEFO DÉRIVÉ du journal (`SUM(delta) GROUP BY lot_id` sur stock_movements, aucune matérialisation par lot), invariant garanti EN transaction par le trigger `stock_movements_lot_solde_positif` (0014, RAISE ABORT LOT_INSUFFISANT) — symétrique par lot du CHECK stock_levels_quantity_positive ; (2) repli sans lot : reliquat non couvert par les lots sort en mouvement sans lotId (stock historique sans lot resterait sinon invendable) ; (3) numéro de ticket = sous-requête MAX+1 scopée boutique, DANS le même batch db.batch que la vente (mono-écrivain SQLite, pas de trou visible) ; (4) idempotence 200/201 : index unique (organization_id, client_request_id) — un retry rejoue la violation d'unicité et renvoie la vente existante en 200 (vs 201 création) ; (5) dépannage autorisé à quiconque peut vendre, aucun flag « caissier autorisé » par utilisateur en v1 (spec muette), tracé au journal (mouvement sale sorti de l'entrepôt source + sourceWarehouseId sur la ligne) ; (6) lecture des ventes ouverte au rôle local cashier de la boutique (réimpression des tickets d'un collègue autorisée — décision v1 documentée).
  Vérifications finales de phase : 243 api + 46 web (23 tests web pré-existants avant Phase 6 déjà supérieurs à l'estimation ~23 du plan initial — aucun test n'était prescrit par le brief T16 pour les 3 nouveaux composants, code repris verbatim) + typecheck + lint + build tous verts ; migrations 0010-0014 confirmées idempotentes (db:migrate:local → « No migrations to apply ») ; drizzle-kit generate → « No schema changes, nothing to migrate » (aucune fuite de trigger/index custom) ; E2E navigateur du brief NON exécuté par l'implémenteur (pas d'accès navigateur interactif dans cette session) — à couvrir avant merge de la PR Phase 6.
PHASE 6 — POS TERMINÉE (16/16 tâches, 243 api + 46 web + typecheck + lint + build verts) — PR à ouvrir
  Minors (différés revue finale): scan pendant focus recherche = double effet visuel (article ajouté + code-barres dans le champ — contrat T12/brief, pas de preventDefault) ; réattachement du listener keydown à chaque rendu pendant le chargement (articles ?? [] non mémoïsé)
Task 16: complete (commits 7025fed..16c6dc5, review clean après 1 fix)
  Fix Important (plan-mandated): ticket imprimable imbriqué sous <main print:hidden> → page BLANCHE à l'impression (le brief affirmait l'isolation, faux) → createPortal(document.body) dans ImpressionTicket (couvre les 3 chemins: vente confirmée, Réimprimer, tickets du jour) + test ticket-recu.test.tsx prouvant le portal (échouerait sur l'avant-fix)
  Arbitrage contrôleur validé par re-revue: dejaEnregistree n'inhibe PAS l'auto-print — seul chemin atteignable = retry après réponse perdue (clé régénérée onSuccess T15 + garde isPending modale), où imprimer est correct ; documenté en commentaire sur onSuccess
  Minors (différés revue finale): double montage ImpressionTicket théorique (inatteignable, overlay bloque) ; TicketsDuJour sans état isError (verbatim brief)
TOUTES LES TÂCHES DU PLAN TERMINÉES (16/16) — état final: 243 api + 47 web, typecheck/lint/build verts, migrations 0010-0014, db:generate idempotent
Prochaines étapes: E2E navigateur → revue finale fable whole-branch → vague de fix → PR
E2E navigateur Phase 6: 12/12 ✅ (+ T16-9 stock_manager conforme) — re-seed complet via UI (D1 locale vierge), parcours caissier bout en bout, FEFO vérifié au journal, écart de fermeture conforme, impression validée structurellement (dialogue natif non interceptable en CDP). Anomalies mineures notées: tuiles parfois incomplètes après navigation répétée (cache React Query, résolu par reload) ; inputs date natifs difficiles à automatiser. Rapport: .superpowers/sdd/e2e-phase-6-report.md
Revue finale whole-branch fable (fb0132d..16c6dc5, 20 commits): Ready to merge = WITH FIXES — 0 Critical, 2 Important même famille « clé (variant, source) non normalisée »: (1) refine Zod dédup `?? ""` vs serveur `?? storeId` → CONFLIT_CONCURRENT insoluble ; (2) definirSource crée des lignes à clé identique (mutations doubles, 400 à l'encaissement). Invariants globaux tous vérifiés tenus (écriture stock unique, gardes SQL symétriques, permissions org-scopées, CMP intact, idempotence bien enchaînée).
Vague de fix revue finale (3b14dc1 + ca9c21f): clé Zod normalisée `sourceWarehouseId ?? storeId` + test 400 ; fusion des quantités dans definirSource (prix de la ligne existante conservé) + tests ; filtre organizationId sur warehouseMembers (register-sessions GET /). Suites finales: 244 api + 49 web, typecheck + lint verts.
Triage revue finale: fix avant merge = les 3 ci-dessus (faits) ; différés Phase 7 (nommés): pagination tickets (route touchée par les rapports P7 de toute façon), tests matrice lecture sessions élargie, test variante inactive isolé, test multi-entrepôts lireLotsDisponibles, mapping route CONFLIT_CONCURRENT/SESSION_FERMEE (harnais de concurrence), MeLike vs Me, apiFetch beforeLoad try/catch, session.isError, piège de focus 2 fuites aux bords, TicketsDuJour isError, gelsLignes N UPDATE (volumétrie), avgCost multi-lignes, anomalie E2E tuiles/cache React Query (invalidation onSuccess seulement) ; ignorés avec justification: dédup 0010 sans test CI (double preuve empirique, échec bruyant), try/catch ×8 (style dépôt), meta/ drizzle (précédent), scénarios forts 0014 (couverts par pos-schema depuis), double lookup T7, cross-org POS lecture (transitif), commentaires/titres cosmétiques T9, sale null-check (inatteignable), borne demain (motif partagé), changerPrix no-op (serveur fait autorité), commentaire definirSource (caduc après fix), regex tests, NaN fail-safe vérifié, scan double effet (contrat assumé), réattachement listener (borné isPending), double montage ticket (inatteignable)
PR #8 ouverte (Phase 6 POS, closes #7). CodeRabbit: 11 findings → 7 fondés corrigés (0a04f46 boutique désactivée /pos/catalogue, e99c019 verrou panier échec ambigu + attente reglages avant impression, 356c2a7 reset panneau mobile money, 366ecbf erreurs réimpression, f2d0653 docs) + 4 écartés justifiés sur la PR (3 recoupent des différés tracés, 1 réfuté par preuve tsc). Suites: 245 api + 55 web.
DÉCOUVERTE CI (3 échecs consécutifs avant fix): vitest-pool-workers démarre par défaut UN processus workerd PAR FICHIER de test (« isolated runtimes ») — à 47 fichiers, la flotte sature les runners GitHub partagés → crashs « Network connection lost » en rafale mid-run, cascades organization_id null via bootstrapOwner. retry global CI (86eb8f4) insuffisant (échecs aux 3 tentatives pendant la rafale) ; vitest maxWorkers SANS EFFET sur le nombre de runtimes du pool (1f6ba8b, retiré) ; FIX RÉEL = poolOptions.workers.singleWorker en CI (927d787) : un runtime, fichiers séquentiels, isolatedStorage conservé — CI verte en 2m08 (plus rapide que les 47 démarrages). retry: process.env.CI ? 2 : 0 conservé en filet.
CI verte + CodeRabbit pass (aucun nouveau commentaire) — PR #8 MERGEABLE, en attente du feu vert utilisateur.
PHASE 6 MERGÉE (PR #8, 2026-07-12, merge commit db943e6) — deploy success (36s), prod vérifiée : health ok, SPA 200, 4 tables POS + 32 triggers + 7 index P6 + 15 migrations (0000-0014) sur la D1 remote. Issue #7 fermée automatiquement. Roadmap à jour sur main.
Bilan Phase 6 : 16 tâches, ~30 commits, 245 tests api + 55 web (vs 183+23 fin P5), E2E 12/12, 5 migrations (0010-0014), 10 nouveaux triggers, 3 vagues de fix intermédiaires + 1 vague finale + triage CodeRabbit (7 corrigés/4 écartés), découverte majeure CI (un workerd par fichier de test → singleWorker en CI).
PHASE 7 — RAPPORTS (plan docs/superpowers/plans/2026-07-12-phase-7-rapports.md, branche feat/phase-7-rapports, base d6688dd)
P7 Task 1: complete (commits d6688dd..da36309, review clean, 0 issue — CMP 120/350/200 re-dérivés contre la formule SQL de stock.ts, non-fuite multi-entrepôts prouvée par toEqual exact)
  Minors (différés revue finale): lots2 en toHaveLength au lieu de toEqual (brief-mandaté) ; arrondi ROUND non exercé par les valeurs choisies (brief-mandaté)
P7 Task 2: complete (commits da36309..6558cfa, review clean, 0 issue — MeLike dérivé structurellement équivalent (vérifié contre roles.ts), session.isError avant le fallback OuvertureCaisse, refetchOnMount sans effet sur panier/scan, invalidation onSuccess conservée)
  Minor (différé revue finale): deux écrans distincts partagent le même handler de retry (destinations.refetch) — lisibilité seulement
P7 Task 3: complete (commits 6558cfa..7dbc8f0, review Approved — 2 fuites fermées, cleanup vérifié, tests régressent sur l'ancien code ; bonus: le focusin global rattrape aussi le raccourci / pendant la modale)
  Note architecturale (à trier en revue finale, pas un défaut actuel): le focusin global de ModalePaiement volerait le focus à un dialogue empilé si un futur flux montait DialogueDepannage en même temps (mutuellement exclusifs par construction aujourd'hui) — garde « topmost dialog » à envisager si empilement un jour ; DialogueDepannage sans role/aria-modal/trap propre
  Minor (différé revue finale): sélecteur SELECTEUR_FOCUSABLES dupliqué dans le test (brief-mandaté, drift possible)
P7 Task 4: complete (commits 7dbc8f0..b7bdf12, review clean, 0 issue — sous-requête sur l'entrepôt SOURCE (test discriminant réserve 300 vs boutique 200), ordre batch instructionsAvant→mouvements→niveaux vérifié, CMP 533 recalculé à la main, coût figé immuable + nouveau gel fonctionnel)
P7 Task 5: complete (commits b7bdf12..833a20f, review Approved sans réserve après 1 fix de couverture — exclusion cashier préservée par composition (vérifié stock-acces.ts), borne au inclusive sans off-by-one, BOM en échappement vérifié dans le diff, test CSV combiné ;/"/\n ajouté (833a20f) ; déviation import type * as schema justifiée)
P7 Task 6: complete (commits 833a20f..6d0aba1, review clean, 0 issue — fan-out évité par 3 requêtes séparées recomposées en JS (vérifié + agrégats recalculés à la main: CA 1400/2000, panier 1133, cash 3100), portée manager restreinte, déviation BOM légitime (Response.text() strippe par spec WHATWG → arrayBuffer, intention préservée))
  Minor (différé revue finale): entrepotDansOrganisation/conditionsVentes non exportés (réutilisés même fichier T7-T8, à surveiller)
  DÉCOUVERTE (pour le plan futur): les assertions startsWith("﻿") sur Response.text() sont impossibles — WHATWG strippe le BOM ; vérifier via arrayBuffer()
P7 Task 7: complete (commits 6d0aba1..2874863, review Approved sans réserve après 1 fix de couverture — valorisations 2000/2000/4000 re-dérivées via la sémantique réelle d'applyMovements, stock_manager voit tout sur CE rapport + 403 ailleurs prouvés, test de régression « inactif inclus » ajouté (2874863: 1200/3200/5200), déviation BOM arrayBuffer légitime)
P7 Task 8: complete (commits 2874863..3822f47, review Approved sans réserve après 2 fixes de couverture — marges re-dérivées (600/900 estime), fan-out impossible (index unique stock_levels vérifié), champ additif sans casse POS (fetchVente destructure sale), stock_manager pur 403 (a50c76f) + hybride local auditor 200 marge:null (3822f47, exerce le court-circuit peutVoirMarge))
  Minor (différé revue finale): formule SQL cout/estime dupliquée verbatim entre reports.ts et sales.ts (identique aujourd'hui, drift possible — helper partagé à envisager)
P7 Task 9: complete (commits 3822f47..51c422a, review clean, 0 issue — total sur mêmes conditions sans join (pas de fan-out), réponse strictement additive, aucun test existant modifié hors prescription)
  Minors (différés revue finale): « page au-delà de la dernière » → [] 200 vérifié par lecture mais non testé (gap du brief) ; jour combiné à du/au ANDé silencieusement au lieu d'être rejeté (code du brief, exclusivité implicite de la décision n°8 non appliquée)
P7 Task 10: complete (commits 51c422a..cbbc4f1, review clean, 0 issue — matrice décision n°10 reproduite et pinnée par tests (dont stock_manager sans ventes du jour), presets sûrs aux bords par normalisation Date native, revokeObjectURL présent)
  À vérifier en revue T11 (transmis): exactitude des noms de fichiers CSV côté appelant (telechargerCsv sans appelant dans T10)
  Minors (différés revue finale): pas de test explicite année bissextile/passage d'année sur periodePreset
P7 Task 11: complete (commits cbbc4f1..250707f, review clean, 0 issue — noms CSV char-for-char contre le serveur (contre-vérifiés), onglets recalculés sur la matrice §4 (le piège blocsTableauDeBord §7 correctement évité — un naïf aurait fuité Valorisation aux locaux), texteMontant ancré sans faux positifs)
  Note valorisation: jour du nom de fichier client = jourLocal vs serveur ISO UTC — design assumé (lien.download prime), auto-signalé
  Minors (différés revue finale ; le 1er TRANSMIS à T12): onglet initial figé par useState(search) — une re-navigation ?onglet différent sans remount ne bascule pas (T12 navigue avec search onglet) ; double role="alert" théorique (export+query en échec simultané)
P7 Task 12: complete (commits 250707f..b952cfc, review clean, 0 issue — portée serveur autoritaire vérifiée sur les 4 endpoints des blocs, pas de boucle caissier, remount de route confirmé par lecture (point T11 clos))
  Minor (différé revue finale): blocs.aucun vrai aussi pour un staff sans affectation (message d'accueil incohérent avec /pos « aucune boutique » — défaut hérité T10, verbatim brief)

Décisions d'architecture Phase 7 (tracées au ledger) :
1. `sale_items.unitCost` nullable, gelé par sous-requête SQL dans le batch de vente (SELECT avg_cost FROM stock_levels) — jamais NULL-check JS post-batch. Rapports valorisent les ventes antérieures à la colonne au CMP courant, marquées estime:true.
2. Format des routes rapports : `?format=csv` sur la MÊME route GET (passe de validation/portée unique, CSV sérialise les lignes JSON). Trois routes : `/reports/sales`, `/reports/valuation`, `/reports/margins`.
3. `porteeRapport(db, orgId, userId, role, rapport)` composé sur `porteeLectureStock` : stock_manager refusé sur ventes/marges (null→403), caissier exclu de tous (portée vide→403). Conditions SQL via `filtrePortee`/`estDansPortee` existants.
4. `/reports/sales` : `du`/`au` obligatoires (400), `groupe=boutique|produit` (défaut boutique), pas de répartition espèces/mobile par produit (niveau vente seulement).
5. `/reports/margins` : GROUP BY variante, `cout = SUM(qty × COALESCE(unit_cost, avg_cost courant, 0))`, LEFT JOIN stock_levels, `estime=true` dès qu'au moins une ligne avait unit_cost NULL.
6. `/reports/valuation` : photographie stock_levels COURANT (pas de période), qty>0 seulement, produits inactifs INCLUS (valeur physique ne disparaît pas), réponse hiérarchique par entrepôt + total global.
7. CSV — nom de fichier recalculé côté client (fetch + blob ne lit pas Content-Disposition cross-origine, headerless → CORS inchangé). Formats : `rapport-ventes-boutiques_<du>_<au>.csv`, `rapport-marges_<du>_<au>.csv`, `rapport-valorisation_<jour>.csv`.
8. GET `/api/v1/sales` étendu : `jour` OU `du`+`au` (ensemble), pagination `page`/`parPage` (défaut 50, max 200), réponse `{ sales, total, page, parPage }` (clé sales conservée, rétrocompatible POS).
9. Marge du détail vente : GET `/api/v1/sales/:id` → `{ sale, marge }` où marge null si pas de droit (champ additif, consommateurs POS intacts).
10. Tableau de bord : visibilité par `blocsTableauDeBord(me)` (fonction pure) — ventes du jour (owner/admin/auditor ou local manager/auditor), alertes+transferts (idem+stock_manager), valeur du stock (org + stock_manager). Bloc vide → renvoi `/pos`.
11. Cache React Query catalogue POS : `refetchOnMount: "always"` + isError avec « Réessayer » ; invalidation onSuccess conservée (anomalie E2E P6 : tuiles incomplètes).
12. **Deux reports DÉFINITIFS documentés au ledger (aucune tâche) :**
    - (a) Mapping route CONFLIT_CONCURRENT/SESSION_FERMEE POST /sales non testé : séquentiellement inatteignable (pré-checks intercepteraient ; harnais vitest mono-écrivain). Vrai harnais de concurrence disproportionné en v1.
    - (b) gelsLignes = N UPDATE par réception à N lignes (volumétrie, hérité 0008) : accepté v1, limite de taille batch D1 documentée au ledger.
P7 Task 13: complete (commits b952cfc..18a9b73, review clean, 0 issue — diff strictement documentaire, checklist vérifiée contre le code réel sans survente, toutes les vérifications rejouées indépendamment à l'identique)
TOUTES LES TÂCHES DU PLAN P7 TERMINÉES (13/13) — état final: 288 api + 76 web, typecheck/lint/build verts, migrations 0000-0015, db:generate idempotent
Prochaines étapes: E2E navigateur → revue finale fable whole-branch → vague de fix → PR
E2E navigateur Phase 7: 8/9 — le ❌ du point 7 (caissier) ARBITRÉ CONFORME par le contrôleur: (a) « / ne redirige pas auto vers /pos » = comportement voulu (décision n°10: renvoi = message+bouton, pas d'auto-redirect, anti-boucle vérifié en revue T12) ; (b) « sidebar Ventes visible pour le caissier » = voulu (matrice lecture ventes P6 décision n°10: cashier lit SA boutique — historique/réimpression), marge bien cachée. LES DEUX TRANSMIS à la revue finale pour contre-vérification. Anomalie hors checklist: bouton Se déconnecter hors viewport par défaut (nav sidebar allongée par la section Ventes — scroll interne requis). Rapport: .superpowers/sdd/e2e-phase-7-report.md
Revue finale whole-branch fable (c353358..18a9b73, 18 commits): Ready to merge = WITH FIXES — 0 Critical, 1 Important (sidebar: overflow-y-auto sur l'aside entier fait sortir Se déconnecter du viewport dès que la nav dépasse h-screen — annule le fix 213e990 ; fix CSS 1 ligne avant merge). Invariants tous tenus (écriture unique unitCost en batch, sécurité org ancrée partout, formule coût/estime identique char-for-char entre les 2 surfaces, rétrocompat POS réelle, perfs sur index existants). Arbitrages E2E du contrôleur CONFIRMÉS (renvoi caissier sans auto-redirect = anti-boucle ; sidebar Ventes caissier = miroir de l'autorité serveur). Incohérence de PLAN notée (décision n°10 vs §4: bloc valorisation sans les locaux — l'implémentation est fidèle à la décision, c'est la décision qui dévie de la matrice qu'elle invoque ; à retrancher post-v1).
Triage minors P7: fix avant merge = sidebar (seul) ; différés post-v1 (ticket groupé recommandé): helper partagé coût/estime, neutralisation formules Excel CSV (=,+,-,@), parPage lu de la réponse, ?onglet source de vérité, harmonisation bloc valorisation §4, ROUND non exercé, page au-delà de la dernière, jour+du/au rejet, bissextile periodePreset, note topmost dialog, blocs.aucun staff sans affectation ; ignorés justifiés: lots2 toHaveLength, retry partagé T2, SELECTEUR dupliqué, helpers non exportés T6, double role=alert, jourLocal vs UTC valorisation (marché UTC+0)
CodeRabbit PR #9: 5 findings → 3 corrigés (8f8e544 limit GET /transfers, 53413de états exclusifs BlocTransferts + limit dashboard, c727538 statut roadmap) + 2 écartés en citant le triage (formules Excel CSV, jour+du/au). CI verte 2m21 (singleWorker stable).
PHASE 7 MERGÉE (PR #9, 2026-07-13, merge becbc55) — deploy success (36s), prod vérifiée: health ok, SPA 200, 16 migrations (0015 unit_cost présent). Roadmap 7/7 sur main (fdcdce6). Issue post-v1 groupée: #10.
Bilan Phase 7: 13 tâches, ~25 commits, 289 api + 76 web (vs 245+55 fin P6), E2E 8/9 (❌ arbitré conforme, confirmé par la revue finale), 5 fixes de couverture en cours de route + 1 fix final (sidebar), 1 migration (0015).
ROADMAP V1 COMPLÈTE — 7 phases, 9 PRs (dont 2 abandonnées/renumérotées), ~290 tests api + 76 web, 16 migrations D1, 32 triggers.

# Ledger Design — feat/design-context-et-colorize-pos (Impeccable, post-v1, PR #11)

Origine : `/impeccable init` → contexte design de la SPA capté (PRODUCT.md, DESIGN.md + sidecar `.impeccable/design.json`, config live, pointeur CLAUDE.md). Register `product`, plateforme `web`, North Star « Le registre du comptable ». Puis audit UX de l'écran de vente POS (`/impeccable audit src/pos/ecran-vente.tsx`) : **11/20**, remédié par passes ciblées jusqu'à **18/20**.

Passes (toutes sur la surface POS : ecran-vente, grille-articles, panier, panneau-ligne, modale-paiement, menu-pos + nouveau modale-confirmation) :
- **colorize** (1edc0f4) : ~30 couleurs Tailwind codées en dur → tokens ; 2 échecs de contraste `gray-400` (2,5:1) corrigés ; menu-pos tokenisé séparément (aa69870, oubli de périmètre).
- **harden** (f599fa2) : `usePiegeFocus` extrait de ModalePaiement (les 2 fuites P6 déjà colmatées) et mutualisé ; nouvelle `ModaleConfirmation` = vrai dialog (role/aria-modal/aria-labelledby/Échap/focus initial) ; `aria-pressed` sur les filtres. +7 tests.
- **adapt** (9c5219e, e2915c3) : cibles tactiles ≥ 44px au doigt via `@media (pointer: coarse)` (chips, boutons ×, recherche, items de menu) — border-box absorbe le padding, dense à la souris.
- **optimize** (20ff286) : `loading="lazy"` + `decoding="async"` sur la grille.
- **polish** (b75baeb) : tuile `shadow-sm` → `ring-1 ring-foreground/10` (règle du filet) ; entête « PANIER » désaturée (eyebrow retirée).

Décisions / arbitrages (tracés au ledger) :
1. Deux tokens sémantiques AJOUTÉS au système (manquaient) : `--success` (monnaie à rendre) et `--warning` (dépannage/source), définis clair **et** sombre + `@theme inline`. La surface participe désormais au mode sombre (non câblé à un toggle aujourd'hui — pré-existant, hors périmètre).
2. `--muted-foreground` 0.556 → **0.52** (suite CodeRabbit) : 0.556 échouait AA sur `bg-muted` (4,34:1 ; OK 4,73:1 sur blanc). Changement de token **app-wide** (tout le muted fonce légèrement) — gain strict, validé visuellement au back-office (dashboard, table produits) : lisible, hiérarchie encre/muted préservée.
3. `usePiegeFocus` restaure le focus au déclencheur à la fermeture (WAI-ARIA APG, garde `isConnected`) — correctif centralisé couvrant paiement + confirmation.
4. **Détection du mode de pointage** (`pointer: coarse`), pas un breakpoint de taille d'écran : on agrandit au doigt sans alourdir la vue souris (back-office), fidèle au « densité par défaut » du DESIGN.md.
5. Surfaces flottantes (menu déroulant) = `bg-popover` + `shadow-md` + `ring-1` (convention `ui/select`), PAS la « règle du filet » stricte des cartes en flux — l'ombre légère est légitime sur un overlay.
6. Contradictions DESIGN.md levées (revue CodeRabbit, docs, 6f9bab2) : « une seule voix » autorise explicitement les aplats d'action/état (bouton primaire, badge) ; « chiffres monospacés » = JetBrains Mono au reçu SEUL, Inter+`tabular-nums` dans l'app (conforme au code + monde clos).

DIFFÉRÉ (décision cadrée, tracé issue #10) :
- **(a) Pipeline images vignettes/WebP** : approche retenue = jSquash resize-on-upload (pattern Workers documenté). **Différé** : bundle WASM ~1,5 Mo incompatible avec le plan Workers **gratuit** (limite 3 Mo compressé) + risque de fragiliser le harness `vitest-pool-workers` (INTOUCHABLE) en important des `.wasm` dans les tests. Cloudflare Image Resizing écarté (images `private` + prod sur `*.workers.dev` = pas de zone custom). Reprise : plan payant ou CF Images, puis spike de de-risk. Cadrage complet en commentaire de #10.

Vérification :
- E2E navigateur (agent-browser, stack locale API+web, D1 seedée) : `pointer-coarse` MESURÉ 28px→44px (émulation CDP `mobile:true`+touch), `aria-pressed` réel, pièges de focus paiement + confirmation (role/aria-modal/aria-labelledby résolu/focus initial « Nouvelle vente »/Échap), vente réelle ticket n°10. Lazy-load NON observable (produits seedés sans image).
- Relecture visuelle back-office (owner) : impact `muted-foreground` app-wide validé, aucune régression.

CodeRabbit PR #11 : **6 findings sur 3 passages**, tous vérifiés puis corrigés (contraste muted AA, restauration focus, hover destructif du logout écrasé par classe partagée, assertion montant → séparateur, 2 contradictions DESIGN.md) ; méthode = vérifier chaque finding, aucun écarté.

DESIGN MERGÉE (PR #11, 2026-07-13, merge commit **55bd4b5**, pas de squash) — **aucune migration D1** (frontend + docs + token), deploy success (migration prod no-op, Worker web redéployé), CI verte 2m22. Branche supprimée. Score audit 11→18. Différé unique : issue #10 (pipeline images).
